{
  "name": "social-api-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter \"@social-api/web\" dev",
    "build": "bun --filter \"*\" build",
    "typecheck": "bun --filter \"*\" typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
